# Tools you can use
1. bash - Execute bash commands on the local system.
   Arguments: {"command": "the bash command to execute"}
2. remember - Store a memory in the system.
    Arguments: {"memory": "the memory to store"}